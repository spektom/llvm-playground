llvm-playground
================

Playing with LLVM for the purpose of building compile-time optimized Jit.

## Prerequisites

 * LLVM >= 8.0.0
 * CMake >= 3.10

## Compiling

    mkdir build
    cmake ..
    make
